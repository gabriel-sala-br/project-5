import { createStitches } from "@stitches/react"; 

export const {config , styled, globalCss, keyframes, getCssText, theme} = createStitches({
    theme: {
        colors: {
            red: "#ff0000",
        },
    },
})